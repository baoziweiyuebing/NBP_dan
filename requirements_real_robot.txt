# Real Robot Deployment Requirements
# 实机部署所需的额外依赖包
#
# 安装方法：
# pip install -r requirements_real_robot.txt
#
# 注意：RealSense SDK和Unitree SDK需要单独安装

# ========================================
# Core Dependencies (仿真环境已有)
# ========================================
torch>=1.13.0
torchvision>=0.14.0
numpy>=1.21.0
scipy>=1.7.0
scikit-image>=0.19.0

# ========================================
# Real Robot Specific Dependencies
# ========================================

# RealSense Camera
pyrealsense2>=2.50.0

# Unitree Robot SDK
# 注意：unitree_sdk2py需要从宇树官方GitHub安装
# git clone https://github.com/unitreerobotics/unitree_sdk2_python.git
# cd unitree_sdk2_python && pip install -e .

# Computer Vision
opencv-python>=4.6.0
opencv-contrib-python>=4.6.0

# 3D Visualization
open3d>=0.16.0

# ROS Support (可选 - 用于RViz可视化)
# 注意：需要先安装ROS系统，然后：
# rospy (通过ROS安装，不在pip中)
# geometry_msgs (通过ROS安装)
# visualization_msgs (通过ROS安装)
# nav_msgs (通过ROS安装)

# Utilities
tqdm>=4.64.0
matplotlib>=3.5.0
pillow>=9.0.0

# ========================================
# Optional Dependencies
# ========================================

# Point Cloud Processing
# trimesh>=4.1.0  # 已在原环境中
# pytorch3d>=0.7.0  # 已在原环境中

# Scientific Computing
# lmdb>=1.3.0  # 已在原环境中
